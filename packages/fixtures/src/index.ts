import type { CourseScope } from "@workbuddy/domain";

export const simulatedCourseScope: CourseScope = {
  tenantId: "org-xinghe-001",
  teacherId: "teacher-lin-001",
  courseId: "course-english-g8-001",
};

export const simulatedCourseVersion = 3;

