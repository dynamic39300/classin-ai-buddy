# 07 — 将 Agent 二级导航嵌入 ClassIn 左侧栏

**What to build:** 教师进入 AI Agent 后，在 ClassIn 左侧栏的 AI Agent 入口下方直接使用新建任务、近期任务和能力资源；右侧 Stage 不再保留独立导航列，并完整用于当前任务工作区。

**Blocked by:** 06 — 完成 M3 综合验收与 Review Gate。

**Status:** completed

- [x] AI Agent 二级导航只在教师 Agent 路由中展开，并位于一级入口下方。
- [x] 新建任务、历史恢复、重命名、置顶、删除和能力入口行为保持不变。
- [x] Work Surface 从 ClassIn Stage 左边界开始，不存在空白或重复的导航列。
- [x] 其他 ClassIn 路由和学生视角不渲染 Agent 二级导航。
- [x] 紧凑桌面窗口仍能访问完整 Agent 导航，且不破坏其他路由的图标栏行为。
